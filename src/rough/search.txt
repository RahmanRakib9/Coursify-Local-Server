// Search Format

{title:{$regex:query.searchTerm,$option:i}}

{language:{$regex:query.searchTerm,$option:i}}

{provider:{$regex:query.searchTerm,$option:i}}

// if searchTerm is given from client side then set that searchTerm otherwise empty string

let searchTerm="" 


if(query?.searchTerm)
{
    searchTerm=query?.searchTerm as string
}