// Error pattern that will send to the client side
/**
 * success
 * message
 * errorSources:[
 * path:" ",
 * message: " ",
 * ]
 * stack (in development phase)
 */

// Algorithm for convert the errors in custom format
-> every errors will go through the global error handler
-> from global error handler error property we can catch the error
-> Then we simply just override the error format as our requirement
